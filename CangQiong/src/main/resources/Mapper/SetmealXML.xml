<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cangqiong.Mapper.SetmealMapper">
    <select id="selectSetmealPage" resultType="com.example.cangqiong.Pojo.Setmeal.SetmealAndDishBody">
        select * from setmeal
            <where>
                <if test="param.categoryId != null and param.categoryId != ''">
                    categoryId = #{param.categoryId}
                </if>
                <if test="param.name != null and param.name != ''">
                    and name = #{param.name}
                </if>
                <if test="param.status != null ">
                    and status = #{param.status}
                </if>
            </where>
        limit #{start} , #{param.pageSize}
    </select>

    <insert id="insertSetmealDish" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO setmeal_dish
            (copies, dish_id, name, price, setmeal_id) VALUES
            <foreach collection="dishList" item="d" separator=",">
                (#{d.copies}, #{d.dishId}, #{d.name}, #{d.price}, #{d.setmealId})
            </foreach>
    </insert>

    <!--批量删除-->
    <delete id="batchDeleteSetmeal">
        delete from setmeal
            where id in
                <foreach collection="ids" item="e" open="(" separator="," close=")">
                    #{e}
                </foreach>
    </delete>

    <delete id="batchDeleteSetmealDish">
        delete from setmeal_dish
            where setmeal_id in
                <foreach collection="ids" item="e" open="(" separator="," close=")">
                    #{e}
                </foreach>
    </delete>

    <update id="updateSetmealDish" >
        update setmeal_dish
        set
            copies = case id
                <foreach collection="list" item="e" separator="">
                    when #{e.id} then #{e.copies}
                </foreach>
            end,
            dish_id = case id
            <foreach collection="list" item="e" separator="">
                when #{e.id} then #{e.dish_id}
            </foreach>
            end,
            name = case id
            <foreach collection="list" item="e" separator="">
                when #{e.id} then #{e.name}
            </foreach>
            end,
            price = case id
            <foreach collection="list" item="e" separator="">
                when #{e.id} then #{e.price}
            </foreach>
            end,
            setmeal_id = case id
            <foreach collection="list" item="e" separator="">
                when #{e.id} then #{e.setmeal_id}
            </foreach>
            end
        where id in
        <foreach collection="list" item="e" open="(" separator="," close=")">
            #{e.id}
        </foreach>
    </update>
</mapper>